# RM-V0_1-FINAL-AUTONOMOUS-CLOSEOUT

Status: **Authoritative resumable Master Goal for the remainder of RunnerMesh v0.1**

## Mission

Take RunnerMesh from the current authoritative protected `main` through the
complete remaining v0.1 path and stop only after either:

```text
RUNNERMESH_V0_1_0=RELEASED
```

or a genuine unresolved product/security/restoration blocker exists.

This is one resumable Master Goal. Do not require the Owner to launch a new
Goal for F1/F2/F3/F4/F5 boundaries. Do not create Roadmap v4. Do not broaden
the frozen v0.1 product contract.

Use the current authoritative:

- protected `main`;
- `goals/RM-V0_1-ROADMAP.md`;
- `goals/RM-V0_1-EXECUTION-STATUS.md`;
- `goals/RM-V0_1-FAST-CLOSEOUT-PLAN.md`;
- `goals/RM-V0_1-F1-P0-H1-OWNER-CORRIDOR.md`;
- F2 through F5 Goal files;
- `dev_governance_files/QUALITY_GATES.md`;
- release criteria;
- accepted ADRs;
- the private H1 Owner bundle and private evidence root.

Refresh remote state first. Preserve unrelated work. Use one writer.

Default execution mode is YOLO, but YOLO never substitutes for the explicit
Owner gates below.

## Governing Owner policy

The historical G11/P0 qualification target has consumed its one permitted
fresh retry. Authoritative state already records:

```text
P0_HISTORICAL_TARGET_RETRY_EXHAUSTED=true
```

Therefore:

```text
DO_NOT_RETRY_HISTORICAL_P0=true
DO_NOT_REPAIR_HISTORICAL_P0_AUDITOR_FOR_V0_1=true
DO_NOT_INVENT_P0_PASS=true
DO_NOT_RECREATE_HISTORICAL_FAILURE_STATE=true
```

The historical target may be retired from the v0.1 qualification authority
only through the explicit Owner gate below.

A fresh official H1 qualification target is the preferred v0.1 path. The
replacement does not waive H1 safety. The replacement must still prove every
live H1 readiness invariant, qualification result, and restoration result.

## Blocker policy for this Master Goal

Do not turn routine infrastructure friction into a new project phase.

```text
WAITING_FOR_OWNER != BLOCKED
WAITING_DOGFOOD != BLOCKED
TRANSIENT_CI_FAILURE != AUTOMATIC_PROJECT_BLOCKER
GENERIC_CODEX_AUDITOR_ADMISSION_FAILURE != PRODUCT_FAILURE
```

Never repeat an unchanged latched blocker.

Do not create new generic Auditor platforms, sandbox frameworks, ACL
frameworks, recovery frameworks, orchestration systems, or transaction
families.

A genuine STOP/BLOCKED is appropriate only for a fresh unresolved condition
such as:

- actual product P0/P1 defect;
- real runner identity/ownership ambiguity;
- security/trust ambiguity that cannot be safely resolved inside current scope;
- H1/H2 live acceptance failure;
- post-mutation restoration failure;
- release artifact/provenance/hash mismatch;
- unavailable external authority that is genuinely required and cannot be
  established.

Perform bounded repair for ordinary implementation defects, CI portability
defects, documentation drift, or configuration mistakes and continue this same
Goal.

## Phase 0 — Materialize and correct the authority path

Materialize this Master Goal, then make the minimum governance correction
required to eliminate the stale historical-P0 deadlock. Do not create Roadmap
v4.

Amend the current Roadmap/Fast-Closeout/Goal/Quality-Gate authority only as
necessary to establish:

```text
H1_ENTRY_AUTHORITY =
    P0_PASS
    OR
    (
        HISTORICAL_P0_TARGET_RETIRED_BY_OWNER=true
        AND
        FRESH_OFFICIAL_H1_TARGET_SELECTED=true
    )
```

F2 must not require the impossible historical `P0=PASS` if the accepted
fresh-target path was used. Its semantic entry authority is:

```text
H1_QUALIFICATION=PASS
H1_RESTORE=PASS
H1_TARGET_AUTHORITY=ACCEPTED
```

If a generic Codex-profile Auditor is used, its admission preflight remains
required for that Auditor mechanism. `AUDITOR_ADMISSION_FAILED` blocks only
that specific audit mechanism; it does not automatically block an alternative
independent verification mechanism satisfying the same active risk
requirement. Do not weaken independent verification.

For H1, prefer the accepted purpose-built deterministic live
collector/readback architecture instead of making generic Codex-Auditor
child-process admission a universal H1 prerequisite. Independent evidence
must remain independent of the mutation helper's own success return.

Use normal protected-main PR flow, exact-head validation, public privacy audit,
and the current docs gate. After the governance/master-goal PR is merged and
current main is healthy, continue automatically. Do not stop merely because
Phase 0 completed.

## Phase 1 — Fresh official H1 target decision and preparation

Perform fresh read-only discovery of the current private host/runner state and
Owner bundle. Do not touch the historical P0 target.

Determine the minimum clean official H1 qualification target strategy. Prefer
reuse of a clearly owned, already-configured healthy official runner when that
can constitute a clean qualification baseline. Otherwise propose the minimum
bounded setup required for a fresh official target.

Never automatically delete or replace a runner, register a new runner, change
runner scope, change work-root ownership, mutate unrelated runners, or expose
private identities.

Before any target-establishment mutation, emit a concise exact privacy-safe
plan and wait in the same session for:

```text
AUTHORIZE_MASTER_FRESH_H1_TARGET
```

This authorization is valid only for the exact displayed plan.

After authorization:

- retire the historical P0 target from v0.1 qualification authority;
- establish or bind the exact fresh official H1 target as authorized;
- harden the exact private evidence ACL needed for H1;
- configure the approved opaque credential reference;
- establish exact runner/local identity bindings;
- establish/verify reserved `runnermesh-admit` selector ownership and
  uniqueness;
- establish/verify the trusted bounded H1 workflow;
- establish routing, rollback, and recovery prerequisites.

Persist a privacy-safe durable checkpoint. Never put private runner IDs, paths,
credential material, private repository/workflow identities, usernames, or
private topology into the public repository. Continue automatically into H1
readiness.

## Phase 2 — H1 live readiness and one-shot qualification

Use the accepted H1 live-adapter and qualification architecture. Do not
redesign G11R. Do not reintroduce historical P0.

Collect fresh LIVE evidence for all eleven readiness fields:

```text
SOURCE_READY
HOST_PRESTATE_READY
GITHUB_AUTHORITY_CONFIGURED
EXACT_RUNNER_IDENTITY_READY
RESERVED_SELECTOR_READY
SELECTOR_UNIQUE
TRUSTED_WORKFLOW_READY
ROUTING_READY
ROLLBACK_READY
RECOVERY_READY
OWNER_GATE_READY
```

Unknown remains fail-closed. Use the purpose-built trusted live collectors,
exact GitHub GET/readbacks, local binding observations, workflow identity
verification, routing observations, rollback proof, and recovery proof.

Do not require generic Codex Auditor child-process admission as the H1 evidence
authority. If existing accepted H1 collector seams need a small deterministic
read-only verifier adapter to produce independently readable evidence,
implement only the minimum verifier inside the existing architecture. Do not
create a generic audit framework.

Require:

```text
H1_LIVE_READINESS=PASS_11_OF_11
```

Then show the exact bounded H1 transaction and wait in the same session for:

```text
AUTHORIZE_MASTER_H1_QUALIFICATION
```

Run exactly one accepted H1 transaction family:

```text
advertise
-> trusted primary routing witness
-> active-job preservation
-> selector withdrawal
-> positive selector-absence readback
-> racing-assignment handling
-> achieved drain
-> re-advertisement
-> reconnect witness
-> automatic baseline restore
-> independent restoration verification
```

Do not destructively signal a normal active Worker. Qualification and
restoration remain separate:

```text
H1_QUALIFICATION=<PASS|FAIL|BLOCKED>
H1_RESTORE=<PASS|FAIL>
```

A post-mutation failure requires restoration before proceeding. Proceed only
when:

```text
H1_QUALIFICATION=PASS
H1_RESTORE=PASS
H1_TARGET_AUTHORITY=ACCEPTED
```

Update the execution ledger in the minimum appropriate PR/accepted
implementation delta, open the PR #33 acceptance gate, and continue
automatically.

## Phase 3 — Productization acceptance

Execute the intent of `RM-V0_1-F2-PRODUCTIZATION-ACCEPTANCE`. Do not stop
merely to hand off to F2.

Use PR #33 if it remains the minimum safe prepared source asset. Reconcile it
with fresh authoritative main only as necessary. Do not rewrite G12-G15 merely
to create new evidence.

Accept the coherent productization scope:

```text
G12=ACCEPTED
G13=ACCEPTED
G14=ACCEPTED
G15=ACCEPTED
```

Require the current risk-appropriate local settled candidate gate,
deterministic privacy/public audit, hosted exact-head Windows and Ubuntu CI,
focused ownership/security delta review, protected-main merge, and healthy
post-merge authoritative main.

No production installation or autostart mutation occurs in this phase. After
acceptance, continue automatically into release-candidate hardening.

## Phase 4 — G15R release candidate and security hardening

Execute the intent of `RM-V0_1-F3-RC-RELEASE-HARDENING` and produce exactly one
final authoritative-main immutable RC. Treat G15R as integration evidence, not
a new implementation project.

Prove:

```text
package
-> checksum/provenance
-> sandbox install
-> active version
-> Agent
-> Tray
-> CLI
-> IPC
-> representative policy/probes
-> lifecycle integration
-> staged update
-> active-job deferral
-> rollback/reconcile
-> uninstall/owned cleanup
```

Close the Named Pipe security debt inside this phase:

```text
NAMED_PIPE_EXPLICIT_DACL=PASS
```

Require current user/logon authorization behavior and deterministic
unauthorized-access denial coverage. Do not create a generic Windows ACL
framework. Preserve immutable workflow dependency pins.

Freeze release artifact naming, provenance, checksums, operator documentation,
and rollback procedure. A security-boundary change still requires genuine
focused independent review. Do not waive that review merely because generic
Codex Auditor admission is unavailable; use another genuinely independent
bounded review mechanism if necessary. Do not launch a full-history audit.

Exit only with:

```text
G15R=PASS
H2_RC_READY=true
RC_IDENTITY=IMMUTABLE
NAMED_PIPE_EXPLICIT_DACL=PASS
RELEASE_SECURITY_DEBT_BLOCKING=0
```

Continue automatically.

## Phase 5 — H2 production cutover

Before production cutover, display the exact immutable RC identity,
installation/cutover actions, affected owned state, and rollback plan. Wait in
the same session for:

```text
AUTHORIZE_MASTER_H2_CUTOVER
```

After authorization, perform only the accepted H2 production cutover. Prove
representative real behavior required by the current H2 contract, including:

- installed Agent/Tray/CLI/IPC;
- user-session activation/autostart;
- human-first behavior;
- exact official runner observation/admission;
- active-job preservation;
- withdrawal/drain/re-advertisement/reconnect;
- required sleep/resume/reboot behavior;
- doctor/runtime isolation;
- rollback availability.

If production mutation fails, restoration is mandatory before any other
development. Require:

```text
H2_CUTOVER=PASS
```

Then immediately record the durable dogfood start checkpoint.

## Phase 6 — Mandatory dogfood and parallel F5 preparation

The minimum ordinary-use dogfood gate remains:

```text
DOGFOOD_MINIMUM=24h
```

Never shorten, round up, infer, or fabricate elapsed time. This is normal
wall-clock workstation usage, not a synthetic stress loop.

During the same Goal, prepare all non-mutating F5 material that is safe before
eligibility: release notes, README/operator docs, artifact/checksum layout,
provenance verification commands, G17 checklist, and publication plan. Do not
publish `v0.1.0`.

If less than the mandatory wall-clock interval has elapsed after all
preparation is complete, report:

```text
STATE=WAITING_DOGFOOD
BLOCKED=false
MASTER_GOAL_COMPLETE=false
```

Do not create another Goal. Do not manufacture work. If this Codex process
remains alive, continue this same Master Goal when the gate matures. If the
process terminates for ordinary operational reasons, relaunch only this exact
Master Goal and resume from the durable dogfood checkpoint. Do not restart the
24h clock merely because Codex restarted.

Once fresh timestamps and ordinary-use evidence prove:

```text
DOGFOOD_HOURS>=24
DOGFOOD=PASS
RELEASE_BLOCKING_LIFECYCLE_FAULT=false
```

continue automatically into G17.

## Phase 7 — G17 release closeout

Execute the intent of `RM-V0_1-F5-RELEASE-CLOSEOUT`. Freeze the exact release
candidate and do not add product features.

Freshly prove release-specific evidence only:

- candidate/main lineage;
- final risk-appropriate hosted CI;
- package contents;
- archive SHA-256;
- member hashes;
- provenance;
- public/privacy audit;
- Named Pipe DACL still accepted;
- no unresolved P0/P1 release defect;
- docs/release notes match candidate;
- dogfood binds to the same immutable RC lineage;
- rollback/recovery documentation current.

Reuse accepted unchanged-risk evidence. Require:

```text
G17=PASS
RELEASE_CANDIDATE_FROZEN=true
H3_ELIGIBLE=true
```

Do not stop merely because G17 passed. Proceed to the publication Owner gate.

## Phase 8 — H3 v0.1.0 publication

Show the exact tag commit, artifacts, accepted hashes, provenance, release
metadata plan, and public privacy boundary. Wait in the same session for:

```text
AUTHORIZE_MASTER_V0_1_0_PUBLICATION
```

After authorization:

- create exact `v0.1.0` tag on the frozen accepted commit;
- publish only already-verified artifacts;
- publish/check SHA-256 and provenance information;
- read back release metadata;
- download and verify public artifacts;
- verify accepted hashes;
- verify no secret/private topology information was exposed;
- perform only the minimum final ledger/status closeout.

Never silently rebuild or substitute a release artifact. A hash mismatch is
release-blocking and cannot be waived. Do not start v0.2.

## Final required state

Do not declare this Master Goal complete until all of the following are true:

```text
HISTORICAL_P0_RETRY_EXHAUSTED=true
HISTORICAL_P0_V0_1_AUTHORITY=RETIRED_BY_OWNER

H1_TARGET_AUTHORITY=ACCEPTED
H1_LIVE_READINESS=PASS_11_OF_11
H1_QUALIFICATION=PASS
H1_RESTORE=PASS

G12=ACCEPTED
G13=ACCEPTED
G14=ACCEPTED
G15=ACCEPTED

G15R=PASS
NAMED_PIPE_EXPLICIT_DACL=PASS
H2_RC_READY=true

H2_CUTOVER=PASS
DOGFOOD_HOURS>=24
DOGFOOD=PASS
RELEASE_BLOCKING_LIFECYCLE_FAULT=false

G17=PASS
H3=PASS

TAG=v0.1.0
PUBLIC_RELEASE=PASS
PUBLIC_ARTIFACT_VERIFY=PASS
RUNNERMESH_V0_1_0=RELEASED
```

Final receipt:

```text
DISPOSITION=
GOAL_ID=RM-V0_1-FINAL-AUTONOMOUS-CLOSEOUT
START_MAIN=
FINAL_MAIN=
HISTORICAL_P0_AUTHORITY=
FRESH_H1_TARGET=
H1_LIVE_READINESS=
H1_QUALIFICATION=
H1_RESTORE=
PRODUCTIZATION_PR=
G12=
G13=
G14=
G15=
G15R=
RC_COMMIT=
RC_ARCHIVE_SHA256=
NAMED_PIPE_DACL=
H2_CUTOVER=
DOGFOOD_START=
DOGFOOD_END=
DOGFOOD_HOURS=
DOGFOOD=
G17=
H3=
TAG=
PUBLIC_RELEASE=
PUBLIC_ARTIFACT_VERIFY=
V0_1_0=
PRODUCTION_MUTATION=
NEXT_ACTION=STOP_AFTER_RELEASE
```

Do not stop at ordinary F1/F2/F3/F4/F5 handoff boundaries. Do not ask the
Owner to launch another Goal. Pause only for the four explicit Owner tokens in
this Master Goal, mandatory dogfood maturation, or a genuine unresolved
technical/safety blocker.
